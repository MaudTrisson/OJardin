{% extends 'base.html.twig' %}

{% block title %}Éditer le jardin {{garden.id}}{% endblock %}

{% block body %}
    <div class="col-xs-6 col-lg-9 m-auto">
        <h2>Éditer le jardin {{garden.id}}</h2>
        {{ form(gardenForm) }}
    </div>


{% endblock %}