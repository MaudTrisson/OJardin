{% extends 'base.html.twig' %}

{% block title %}Éditer le magasin {{store.id}}{% endblock %}

{% block body %}
    <div class="col-xs-6 col-lg-9 m-auto">
        <h2>Éditer le magasin {{store.id}}</h2>
        {{ form(storeForm) }}
    </div>


{% endblock %}